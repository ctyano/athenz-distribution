name: Close stale issues and pull requests

on:
  schedule:
    - cron: '17 0 * * *'
  workflow_dispatch:

jobs:
  stale:

    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write

    steps:
      -
        name: Close stale issues and pull requests
        uses: actions/stale@v10
        with:
          days-before-issue-stale: 23
          days-before-issue-close: 7
          stale-issue-label: stale
          stale-issue-message: 'This issue has had no activity for 23 days. It will be closed in 7 days if there is no comment or update.'
          close-issue-message: 'Closing this issue because it has had no activity for 30 days.'
          exempt-issue-labels: keep-open
          days-before-pr-stale: 23
          days-before-pr-close: 7
          stale-pr-label: stale
          stale-pr-message: 'This PR has had no activity for 23 days. It will be closed in 7 days if there is no comment or update.'
          close-pr-message: 'Closing this PR because it has had no activity for 30 days.'
          exempt-pr-labels: keep-open
          exempt-draft-pr: true
