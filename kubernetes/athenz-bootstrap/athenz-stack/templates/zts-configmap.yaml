---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "athenz-stack.fullname" . }}-config-{{ $hash }}
data:
{{- range $k, $v := .Values.config.literals }}
  {{ $k }}: {{ $v | toString | quote }}
{{- end }}
{{- range $path, $_ := .Files.Glob "zts_config/*" }}
  {{ base $path }}: |-
{{ .Files.Get $path | indent 4 }}
{{- end }}
